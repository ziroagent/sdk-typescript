# basic-chat

The smallest possible Ziro AI SDK program: one `generateText` call followed
by one `streamText` call against OpenAI.

```bash
export OPENAI_API_KEY=sk-...
pnpm --filter @ziro-ai/example-basic-chat start
```
