# basic-chat

The smallest possible ZiroAgent SDK program: one `generateText` call followed
by one `streamText` call against OpenAI.

```bash
export OPENAI_API_KEY=sk-...
pnpm --filter @ziroagent/example-basic-chat start
```
