---
'@ziro-agent/eval': minor
'@ziro-agent/cli': patch
---

Declarative eval v1.1: **`llmJudge`** in `*.eval.json` with **`judgeModel.mock`**, **`*.eval.yaml`** loader, and expanded recording-regression tests. CLI loads YAML eval datasets.
