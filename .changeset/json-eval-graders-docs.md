---
'@ziro-agent/eval': patch
---

**Declarative `*.eval.json`:** support **`contains`** and **`regex`** graders (with options) alongside **`exactMatch`** for `runKind: "modelText"`. Export **`JsonDatasetDoc`** type from package index.
